import { elevation } from "@shared/tokens";

export const preset = {
  fullWidth: false,
  roundedFull: true,
  overrideButtonContainerStyles: "aspect-auto px-3",
  disablePressedEffect: true,
  defaultEffect: elevation.floating,
};
